import {
  ArrowLeft,
  Play,
  Check,
  AlertTriangle,
  Pause,
  Square,
  MoreVertical,
  ArrowUp,
  ArrowDown,
  ChevronDown,
  ChevronRight,
  Search,
  Info,
  CheckCircle,
  XCircle,
  AlertCircle,
} from "lucide-react";

export const Icon = {
  ArrowLeft,
  Play,
  Check,
  AlertTriangle,
  Pause,
  Square,
  MoreVertical,
  ArrowUp,
  ArrowDown,
  ChevronDown,
  ChevronRight,
  Search,
  Info,
  CheckCircle,
  XCircle,
  AlertCircle,
};

export type IconName = keyof typeof Icon;
